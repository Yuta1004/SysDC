unit compiler.parser;

from compiler.structure import SysDCSystem;
from compiler.tokenizer import TokenList;

module Parser {
    parse(tokens: TokenList) -> SysDCSystem {
        @return system

        @spawn system: SysDCSystem {
            use tokens;
        }
    }
}
