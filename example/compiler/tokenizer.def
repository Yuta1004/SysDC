unit compiler.tokenizer;

from std import String;

data TokenList {}

module Tokenizer {
    tokenize(program: String) -> TokenList {
        @return tokens

        @spawn tokens: TokenList {
            use program;
        }
    }
}
